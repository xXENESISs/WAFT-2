name: Repair WAFT 0.16.0 builder

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - .github/workflows/repair-waft-0160-builder.yml

permissions:
  contents: write

# This second revision intentionally triggers the repair after workflow registration.
jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Repair malformed shader string boundaries
        run: |
          python3 <<'PY'
          from pathlib import import Path
          PY
